const arr = [3,5,6,7,5,3,24,2344,345];

const arraylenght = arr.length

for(let i  = 2; i < arraylenght; i++){
    if (arr % i  === 0)
    {
        console.log(arr[i]);
    }else {
        console.log(arr[i]);
    }
}